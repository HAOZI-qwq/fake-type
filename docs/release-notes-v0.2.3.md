## Fake Type v0.2.3

这一版修复补全发生在输入队列中时的最后几个进度竞态，避免预设字符在补全后再次输出。

### 修复

- 补全替换在当前预留字符尚未落盘时，从该字符的真实起点重新对齐
- 目标文件存在排队输入、但当前正在处理其他文件时，也能取消过期预留字符
- 自身的选区替换和 LF / CRLF 等价写入不会被误判为外部补全
- 补全触发后重新计算取消队列造成的进度变化，避免索引停在旧位置
- 多光标删除按实际删除的逻辑字符数量回退

### 验证

- 新增活动写入未落盘、跨文件队列取消和补全替换边界测试
- 共 25 项自动化测试全部通过
